# Leave this file unused for normal local and LAN testing.
# The client automatically uses ws://<current-browser-host>:2567.
#
# Later, when the server is deployed, create client/.env.local and set:
# VITE_WS_URL=wss://your-render-server.onrender.com
