# Copy this file to client/.env.production only if you prefer a saved build setting.
# The included scripts/build-itch.ps1 script sets this automatically from your
# final Render URL, so normally you do not need to create the real file.
VITE_WS_URL=wss://YOUR-RENDER-SERVICE.onrender.com
